# R25.9 — Portfolio Guardrails + Sizing Caps

**Date:** 2026-02

## Summary

Portfolio guardrails and sizing caps (advisory-first): configurable limits for open options/shares positions, symbol exposure, notional per symbol, cash reserve, options risk per trade, and sector exposure (advisory only). Request-time evaluation from account snapshot; hard caps suppress ENTRY from Action Needed with safe reason codes. Dashboard and System Diagnostics show a Guardrails card. Manual execution only; no FAIL/WARN in UI or decision artifacts.

## What changed

### Backend

- **Config:** Portfolio guardrails config in settings (or config module) with defaults: MAX_OPEN_OPTIONS_POSITIONS (6), MAX_OPEN_SHARES_POSITIONS (10), MAX_SYMBOLS_EXPOSURE (12), MAX_NOTIONAL_PER_SYMBOL_PCT (15), MIN_CASH_RESERVE_PCT (25), OPTIONS_MAX_RISK_PER_TRADE_PCT (2), SECTOR_EXPOSURE_ADVISORY_PCT (35). `get_guardrails_config()` returns a dict.
- **Engine:** `app/core/portfolio/guardrails_r259.py` — `compute_portfolio_metrics(snapshot)` and `evaluate_guardrails_for_entry(metrics, proposed_entry)`; deterministic reason ordering; status OK/Advisory/Blocked (safe labels only).
- **Action Needed:** ENTRY items that would violate hard caps are suppressed; safe reason shown (e.g. Cash reserve rule, Max open positions reached). API schema stable; no FAIL_/WARN_ in payload.
- **System Health:** `/api/ui/system-health` (or equivalent) extended with guardrails block: status, key metrics, limits.

### Frontend

- **Dashboard:** Guardrails card — cash reserve %, open options/shares counts, symbols exposure, max symbol notional %; status OK/Advisory/Blocked (safe labels only).
- **System Diagnostics:** Guardrails card with same metrics + status.
- **Action Needed:** Suppressed ENTRY shows safe reason; no raw FAIL/WARN in DOM.

### Tests

- Backend: `tests/test_r259_guardrails.py` — determinism, cash reserve blocks ENTRY, max options blocks, sector advisory only, action-needed no ENTRY when blocked, no forbidden substrings in JSON.
- Frontend: Guardrails card renders (Dashboard + System Diagnostics); document.textContent has no FAIL/WARN.

## Verification

- `out/verification/R25.9/notes.md`: gate tails + UAT checklist.
- No FAIL_/WARN_ in decision_latest.json or API responses.
