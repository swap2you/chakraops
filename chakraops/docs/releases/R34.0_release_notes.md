# R34.0 Release Notes — Canonical Live Cutover + Consolidated R32–R34 Blocker Remediation

**Branch:** `release/R31-R35-program` (program milestone 4)
**Status:** COMPLETE (pending final external validation). Final cutover pass delivered + gate-verified: transaction-safe weekly refresh, complete ORATS application-path redaction, live sector enforcement, **rendered** canonical cutover (Dashboard/Today/Symbol), shared-table DOM fix, Backtest SIMULATION label, positions pagination, navigation grouping. **H-5 CLOSED.** Awaiting final Claude/Codex/Cowork validation before R35.0.

## Final cutover pass (2026-06-22) — R34.0 COMPLETE

The remaining required outcomes from the cutover are now delivered and gate-verified. This supersedes the "STAGED / deferred" notes below for the Phase 7 required items.

- **Transaction-safe weekly universe refresh:** one cross-process lock (`app/core/universe/refresh_lock.py`) spans idempotency → snapshot → overlay → history → completion; overlay/history writes are atomic (temp + flush+fsync + `os.replace`); a journal supports deterministic recovery/rollback after interruption; rollback/recovery failure raises an explicit `WeeklyRefreshCriticalError`. Concurrent same-week calls produce exactly one applied refresh + one history record. Admin route returns controlled APPLIED/SKIPPED_IDEMPOTENT/FAILED/CRITICAL status. No scheduler (R35 owns scheduling).
- **Complete ORATS redaction:** sanitized at exception construction; `RequestException` wrapped (`from None`, no bare token-bearing rethrow); response bodies/snippets/headers/diagnostics/boot-probe/HTTP-error paths redacted across all ORATS application code. Fake-secret regression tests; secret scan 0 hits in tracked code and evidence; local `.env` untouched.
- **Live sector enforcement:** symbol→sector + existing sector exposure mapped from portfolio data; profile sector caps enforced; incremental CSP/share-buy BLOCKED when sector data is unavailable; existing-share covered calls allowed and flagged `SECTOR_DATA_UNAVAILABLE_EXISTING_POSITION`; deterministic reason codes.
- **Rendered canonical cutover:** `AuthoritativeRecommendations` is the Dashboard/Today operator-facing primary (actionable/WATCH/BLOCKED/STAY_IN_CASH, active profile, manual-only wording, freshness, provider health, safe reason labels, per-suggestion + combined capital, deployable cash, additivity, combined-capital warning). Legacy `top_options`/`top_shares` are demoted to a collapsed `Diagnostics — non-authoritative legacy output` section. Stale/unavailable canonical never renders a legacy primary. Page-level tests prove all of the above.
- **Symbol Diagnostics:** canonical decision rendered primary; legacy diagnostics explanatory; absent symbols show a NOT-EVALUATED state + Recompute (never a generic "Failed to load"); no raw FAIL_/WARN_/PASS codes.
- **Frontend correctness:** shared-table `<tr>`-in-`<tr>` DOM fix (centralized in `Table.tsx`); Backtest `SIMULATION — NOT A LIVE RECOMMENDATION` label; positions pagination; logical navigation grouping (Daily/Research/Account/Insights/Admin); understandable loading/empty/stale/unavailable/failure states.

**Gates (final pass):** backend 1200 passed / 1 skipped; frontend 334 passed / 18 skipped; build PASS (only pre-existing chunk-size + dynamic/static-import warnings, deferred post-R35); secret scan 0 hits.

**H-5: CLOSED** after rendered-UI cutover page tests passed. Physical legacy-module retirement deferred to post-R35.

---


## Consolidated R32–R34 blocker remediation (2026-06-22)

Codex consolidated R32–R34 = **BLOCKED**; Claude R34 = APPROVED WITH NON-BLOCKING NOTES (narrow API cutover only); Cowork = PASS WITH NOTES (narrow API cutover only; no true browser rendering). This pass closes the concrete blockers:

- **Operational weekly universe refresh (was preview-only):** `apply_weekly_universe_refresh()` computes the deterministic weekly universe, applies it via the canonical overlay store (atomic bulk `apply_effective_universe`), and appends exactly one history record. Idempotent per ISO week; atomic with overlay rollback if the history append fails; fails loudly. Admin trigger: `POST /api/ui/universe/weekly-refresh/apply` (supports `dry_run`). R35 still owns scheduling.
- **ORATS credential log redaction:** central `app/core/security/redact.py` (`redact_secrets`/`redact_params`/`safe_provider_error`) wired through every ORATS request-failure/log/exception path and the data-health/boot-probe/503 surfaces. Token query params, bearer/api-key headers, and credential-bearing URLs are scrubbed; endpoint/status/classification preserved. Regression tests use a FAKE token; the ignored local `.env` is never read or printed.
- **Fail-closed canonical computation:** `/api/ui/action-needed` and the symbol-diagnostics attach helper never claim `decision_source = canonical_decision_engine` when canonical output is absent. On failure/missing/invalid-profile they return `canonical_decision_engine_unavailable` with empty actionable recommendations and a reason code (`CANONICAL_ENGINE_UNAVAILABLE` / `CANONICAL_ARTIFACT_MISSING` / `CANONICAL_PROFILE_INVALID`); legacy lists are never promoted. `/api/view/daily-overview` normalized with canonical decision-source markers.
- **Missing-cash and sector safety:** available cash is never inferred from total equity; when cash is unknown, cash-consuming CSP/share-buy are non-actionable and `AVAILABLE_CASH_UNKNOWN` is surfaced; covered calls may still proceed; sector concentration that cannot be evaluated is flagged (`SECTOR_UNKNOWN`), never silently ignored.

## Summary

R34.0 closes the Claude R33 **BLOCKED** verdict by making the canonical R33 decision
engine the **authoritative producer of the primary live recommendation** at the API/data
layer. The legacy evaluator/ranking stack no longer defines the primary action: it is
relabeled non-authoritative/diagnostic. The R32 stale-data gate is enforced on the live
actionable path, recommendation-set capital safety is surfaced, and the mandatory
persistence decision is documented (RETAIN current stack, no migration).

This is an **advisory, manual-only** system. Nothing routes orders; there is no broker
integration, no auto-trading, and no silent data fallback.

## Delivered

- **Canonical live cutover (H-5, API/data layer):**
  - `app/core/decision_engine/legacy_adapter.py` maps canonical `DecisionOutput` → live UI
    shapes (no `FAIL_`/`WARN_` leakage; `recommended_by = canonical_decision_engine`).
  - `app/core/decision_engine/live_service.py` builds canonical `DecisionInput`s from the
    already-persisted v2 decision artifact, runs the deterministic engine **in-process at
    request time** (no ORATS calls, no fallback), and returns one authoritative block.
  - `/api/ui/action-needed` now returns `authoritative_recommendations` (canonical),
    `capital_safety`, `decision_source`, and `active_profile`; legacy `options`/`shares`
    lists are kept as `legacy_lists_role = diagnostic_non_authoritative`.
  - Symbol Diagnostics returns a `canonical_decision` block + `decision_source`; Today
    summary advertises `decision_source` and the canonical action-needed source.
- **Stale-data safety:** the R32 `stale_data_gate` is wired through the canonical engine on
  the live actionable path. Missing/stale critical data yields BLOCKED/WATCH/STAY_IN_CASH —
  never ACTIONABLE.
- **No conflicting primary:** the response declares a single authoritative `decision_source`;
  legacy is non-authoritative.
- **Recommendation-set capital safety:** `per_suggestion_not_additive` label, total capital
  required for the displayed set, deployable capital after the profile cash buffer, and an
  over-capital warning. No assumed leverage/margin. R33 sizing math unchanged.
- **API hardening:** invalid `profile`/`profile_overrides` → HTTP 422 (not 500).
- **Frontend:** authoritative types (`AuthoritativeRecommendations`, `CapitalSetSafety`,
  `CanonicalLiveItem`) and `useActionNeeded(profile?)` carrying the active profile.
- **Persistence decision:** RETAIN SQLite + append-only JSONL; no migration (see
  `docs/ai/releases/R34.0/persistence_decision.md`).

## Gates (latest, this remediation pass)

- Backend: 1169 passed, 1 skipped.
- Frontend tests: 315 passed, 18 skipped.
- Frontend build: PASS (known M-13 chunk-size + UniverseAdminPage nested-table warnings — staged Phase 6).

## Honest scope / deferred

**Phase 5 (rendered visual cutover) and Phase 6 (full product scope)** — dashboard/navigation
consolidation, portfolio/position experience, universe/data-health UI, backtest engine,
journal/retention/reporting reconciliation, and the broader frontend-quality overhaul (incl.
nested-`<tr>` warnings and M-13 bundle work) — are **STAGED and not claimed complete**. The
rendered UI still shows legacy primary lists; only the API/data contract (action-needed,
symbol-diagnostics, daily-overview) is canonical/fail-closed today.

## H-5

**OPEN.** Fail-closed and authoritative at the API/data-contract layer (evidenced by tests).
Rendered-UI cutover (Phase 5) and legacy module physical retirement are not done; H-5 closes
only after rendered-UI cutover tests pass.

## Review status

- Codex: consolidated R32–R34 **BLOCKED** — cited blockers remediated this pass; re-review required.
- Claude: APPROVED WITH NOTES (narrow API cutover only); re-review required after Phase 5.
- Cowork: PASS WITH NOTES (narrow API cutover only; true browser rendering unavailable); rendered-UI UAT pending.

## Liquidity provenance

The artifact does not persist per-contract OI/volume/spread. Where the upstream batch
Stage-2 gate set `option_liquidity_ok == True`, the live service treats the contract as
liquidity-validated upstream and tags items with `LIQUIDITY_VALIDATED_UPSTREAM` (visible in
`reason_codes`); otherwise the canonical liquidity gate blocks. R33 math unchanged.
