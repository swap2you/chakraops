# R24.1 — Actionable Trading Workflow v1 — Granular Runbook

Use **one step per session** to avoid OOM. After each step, close unused tabs; only open files needed for the next step.

---

## Pre-check (do first after reopen)

- [ ] Root `.cursorignore` exists with patterns from `CURSOR_OOM_AVOIDANCE.md` §1.
- [ ] Optionally: "Don't restore editors" was checked when reopening Cursor after OOM.

---

## Part 2 — Slack (already implemented; verify only)

| Step | Task | Command / file |
|------|------|----------------|
| 2.1 | Run Slack R24.1 tests | `cd chakraops; python -m pytest tests/test_r241_slack_actionable_messages.py -v --tb=short` |
| 2.2 | If fail: open only `slack_dispatcher.py` and test file; fix and re-run 2.1 | — |

---

## Part 3 — Dashboard + deep-link (already implemented; verify only)

| Step | Task | Command / file |
|------|------|----------------|
| 3.1 | Dashboard uses GET /api/ui/action-needed | Already in `DashboardPage.tsx` (actionNeeded, top_options, top_shares, accordion_id in link). |
| 3.2 | Symbol page opens accordion from URL | Already in `SymbolDiagnosticsPage.tsx` (accordionFromUrl, initialAccordionId). |
| 3.3 | Add/run Dashboard test for action-needed | Open only `DashboardPage.test.tsx`; mock `useActionNeeded`, assert action-needed card has options/shares rows and link href has `tab=` and `accordion=`. Then: `cd frontend; npm run test -- --run DashboardPage` |
| 3.4 | Add/run Symbol page test for accordion param | Open only `SymbolDiagnosticsPage.test.tsx` or score test; render with `?accordion=trade` or `accordion=trade-plan`, assert correct accordion open. Then run SymbolDiagnosticsPage tests. |

---

## Part 4 — Retention docs + verification

| Step | Task | Command / file |
|------|------|----------------|
| 4.1 | Document retention in release notes | Open only `chakraops/docs/releases/R24.1_release_notes.md`; add DECISION_ARCHIVE_MAX / DECISION_HISTORY_KEEP and where retention runs (e.g. evaluation_store_v2). |
| 4.2 | Document retention in checklist | Open only `chakraops/docs/releases/RELEASE_CHECKLIST.md`; add R24.1 retention bullet. |
| 4.3 | Backend gate | `cd chakraops; python -m pytest tests/ -v --tb=short` → paste tail (or full) into `out/verification/R24.1/notes.md`. |
| 4.4 | Frontend test gate | `cd frontend; npm run test -- --run` → paste tail into `out/verification/R24.1/notes.md`. |
| 4.5 | Frontend build gate | `cd frontend; npm run build` → paste tail into `out/verification/R24.1/notes.md`. |
| 4.6 | UAT checklist | In `out/verification/R24.1/notes.md` add checklist: Action Needed options+shares, deep-link tab+accordion, Slack dedupe, decision_latest.json code-only (no FAIL_/WARN_). |

---

## R24.1 COMPLETE

- **All Parts 2–4 implemented.** Gates run and pasted in `out/verification/R24.1/notes.md`. RELEASE_CHECKLIST R24.1 section marked [x].
- **Handoff for ChatGPT/agent:** Use `docs/master/R24.1_HANDOFF_FOR_AGENT.md` — contains full prompt, implementation table, files changed, proof points, gate commands, and OOM-safe steps (one step per session).
- **Optional:** Run UAT checklist in notes.md when ready.

---

## File list (minimal open set per step)

- Step 2.x: `chakraops/app/core/alerts/slack_dispatcher.py`, `chakraops/tests/test_r241_slack_actionable_messages.py`
- Step 3.3: `frontend/src/pages/DashboardPage.test.tsx`, `frontend/src/pages/DashboardPage.tsx`
- Step 3.4: `frontend/src/pages/SymbolDiagnosticsPage*.tsx`
- Step 4.1–4.2: `chakraops/docs/releases/R24.1_release_notes.md`, `RELEASE_CHECKLIST.md`
- Step 4.3–4.6: `out/verification/R24.1/notes.md` (and terminal for commands)
