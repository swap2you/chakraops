# R27.7 — Phase 8 Portfolio & Position Management v1 (shares + CC readiness + portfolio snapshot)

**Branch:** release/R27.7  
**Manual-only.** No broker automation. No new strategies.

## Objectives

- Extend shares positions store (SQLite under DATA_DIR) so per-position cost basis and unrealized P/L are computable reliably.
- Add request-time portfolio enrichment for shares: mark_value/source/age, unrealized_pl, pct_return, days_held, cc_eligible (bool), cc_eligible_reason.
- Add advisory notification CC_ELIGIBLE (dedupe by symbol while NEW/ACKED).
- Portfolio page: cc_eligible badge + filter; Unrealized P/L and %; link to Trade Ticket with strategy=CC when eligible.
- Notifications page: render CC_ELIGIBLE with safe labels; deep link to /ticket?symbol=...&strategy=CC&action=OPEN.

## Non-negotiables

- Decision artifacts remain code-only; no prose/UI strings persisted; no FAIL_/WARN_ tokens in decision_latest.json.
- UI must never show raw FAIL/WARN words/codes; safe labels only.
- Determinism: computed portfolio metrics stable for same inputs (sorted, request-time only unless explicitly stored in SQLite under data/).
- Manual-only; no broker automation.
- Gates must pass; verification in out/verification/R27.7/notes.md + UAT checklist.

## Scope

1. **Backend:** Shares store (cost basis from avg_cost × quantity); enrichment (mark_value, mark_source, mark_age_sec, unrealized_pl, pct_return, days_held, cc_eligible, cc_eligible_reason); CC_ELIGIBLE notification with symbol dedupe (NEW/ACKED).
2. **Frontend:** Portfolio page — CC eligible badge + filter, Unrealized P/L and %, link to /ticket?symbol=...&strategy=CC&action=OPEN when cc_eligible; Notifications — CC_ELIGIBLE safe label + deep link.
3. **Tests:** Backend cc_eligible rules and notification dedupe; no FAIL_/WARN_ in payloads; frontend portfolio CC eligible and no FAIL/WARN in DOM.
4. **Docs:** This file, R27.7_release_notes.md, RELEASE_CHECKLIST R27.7 block, out/verification/R27.7/notes.md.

## UAT checklist

- [ ] Portfolio shares: Unrealized P/L and % shown; cc_eligible badge and filter work; link to Ticket (CC) when eligible.
- [ ] Notifications: CC_ELIGIBLE shows safe label; deep link opens Ticket with symbol and strategy=CC.
- [ ] No raw FAIL/WARN in UI or decision artifacts.
- [ ] Gates pasted in notes.md.
