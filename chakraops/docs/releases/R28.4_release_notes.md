# Release R28.4 — Live open mirror to unified positions DB

**Date:** 2026-03  
**Branch:** release/R28.4  
**Goal:** Mirror LIVE OPEN (shares + options) into unified positions DB; reconcile health includes live-open counts; safe labels only.

---

## What changed

### Backend

- **positions_unified_store_r279.py**
  - **mirror_live_shares_open_to_unified(share_position)** — R28.4: Idempotent upsert of live SHARES open into positions_open with stable id `live_shares_{id}`. instrument_type=SHARES, is_paper=0.
  - **_mirror_live_shares_close** — R28.4: Before inserting closed row, DELETE from positions_open the live SHARES row for that symbol so open→close lifecycle is one open removed, one closed inserted.
  - **get_positions_unified_reconcile_health** — R28.4: Added live_shares_open_count, live_options_open_count, unified_open_live_shares_count, unified_open_live_options_count. status remains OK or Review only (no FAIL/WARN/PASS).

- **ui_routes.py**
  - After successful POST /shares/positions/{symbol} (upsert_share_position), call mirror_live_shares_open_to_unified(pos) and ensure_reconcile_advisory_notification().

### Tests

- **test_r284_positions_unified_live_open_mirror.py** — test_live_shares_open_mirrors_to_unified_idempotent, test_live_options_open_mirrors_to_unified_idempotent, test_reconcile_health_includes_live_open_counts_safe_labels_no_fail_warn_pass, test_mirror_live_open_does_not_write_decision_latest.

### Frontend

- No code changes. System Diagnostics already consumes positions_unified_reconcile; new keys (live_shares_open_count, etc.) are additive; safe labels only.

---

## Backward compatibility

- Reconcile health response gains new keys; existing keys unchanged. status still OK or Review. Notifications and close/roll behavior unchanged.

---

## Gate counts (evidence)

- R28.4 module: 4 passed.
- Full backend: 943 passed, 1 skipped in 127.06s.
- Frontend tests: 249 passed, 18 skipped.
- Frontend build: OK in 8.08s.
- Evidence: [out/verification/R28.4/notes.md](../../../out/verification/R28.4/notes.md)
