# R30.7 Release Notes — Runtime-File Hygiene Cleanup

## Title

R30.7 — Runtime-file hygiene cleanup

## Goal

Stop tracking three grandfathered runtime output files under `out/`. The files remain present locally and are already covered by `.gitignore`. Existing Git history is not rewritten.

## Files Untracked

The following files are removed from the Git index only (physical files preserved on disk):

- `out/decision_latest.json`
- `out/mark_refresh_state.json`
- `out/notifications.jsonl`

## Documentation Updated

| File | Change |
|------|--------|
| `chakraops/docs/releases/R30.7_requirements.md` | Created |
| `chakraops/docs/releases/R30.7_release_notes.md` | Created (this file) |
| `chakraops/docs/releases/RELEASE_CHECKLIST.md` | R30.7 section added |
| `docs/master/KNOWN_ISSUES.md` | Issue #4 updated: resolved by R30.7 — three `out/` runtime files no longer tracked, remain local and ignored, Git history not rewritten |
| `docs/master/CURRENT_STATE.md` | Updated: current branch and mode |

## User-Visible Changes

None.

## API / Backend Changes

None.

## Frontend Changes

None.

## Runtime Behavior

The three files continue to exist on the local filesystem and are written by the application at runtime as before. They are simply no longer tracked by Git. `.gitignore` already covers `out/`, so they will not be accidentally re-staged.

## Verification Path

`out/verification/R30.7/notes.md`

## Rollback

Tag `chakraops-r30.6.1` is the rollback point.

To roll back:
1. Re-add the three files to the Git index (requires explicit operator approval): `git add -f out/decision_latest.json out/mark_refresh_state.json out/notifications.jsonl`.
2. Or revert the R30.7 commit.
3. No code changes to revert.

## Explicit Note

No Git-history rewrite. The three files remain visible in commits prior to R30.7. Only future tracking is stopped.
