# Release R28.0 — Paper write mirror + reconcile health + Positions UI upgrades

**Date:** 2026-03  
**Branch:** release/R28.0  
**Goal:** Mirror paper positions into unified DB; add reconcile health; upgrade Positions page with Source, Mark/Unrealized, safe labels.

---

## Backend

- Paper open/close write paths call unified-store upsert helper (positions_open / positions_closed); idempotent keys; deterministic ordering preserved.
- System-health block `positions_unified_reconcile`: paper source vs unified DB counts; status `OK` or `Review` (safe labels only).

## Frontend

- Positions page: Source column (LIVE/PAPER); Mark and Unrealized for paper rows when API provides; Ticket/Journal/Paper close links; safe labels only.

## Tests

- Backend: test_paper_open_mirrors_to_unified_idempotent; test_paper_close_mirrors_to_unified_idempotent; test_reconcile_health_safe_labels_no_fail_warn; existing R27.9 determinism/filter tests; no decision_latest writes.
- Frontend: Positions columns, mark/unrealized for paper, filters, no FAIL/WARN in document.

## What changed

- **Backend:** Paper open/close write paths (paper execute + paper/close) call `mirror_paper_open_to_unified` / `mirror_paper_close_to_unified`; idempotent upserts into positions_open/positions_closed; system-health block `positions_unified_reconcile` (paper vs unified counts; status OK or Review).
- **Frontend:** Positions page: Source column (LIVE/PAPER); Mark/Unrealized when API provides; Paper link for paper rows; safe labels only.
- **User-visible:** /positions shows Source and Paper link; Mark/Unrealized for paper when API returns them.
- **API:** No new endpoint; system-health includes `positions_unified_reconcile`.

## Gate counts (evidence)

- Backend R28.0: 4 passed. Full suite: 921 passed, 3 skipped.
- Frontend tests: 246 passed, 18 skipped.
- Frontend build: built in 7.49s.
- Evidence: [out/verification/R28.0/notes.md](../../../out/verification/R28.0/notes.md)
