# R24.7.0 — Repo Inventory + Cleanup Plan (NO deletions)

## Scope

Process-only release: document repo layout, define what to keep vs archive, and prepare for future safe cleanup. **No files are deleted or moved in this release.**

## Requirements

1. **Repo architecture map**
   - Create `docs/master/REPO_ARCHITECTURE_MAP.md` with: top-level tree (1–2 levels), one-liner purpose per major folder, "Must keep" list (explicit paths), "Archive candidates" list with justification, "Delete candidates" only if proven unused (with grep checks), naming conventions and where to add new docs/tests, and out/ whitelist summary plus what is safe to clean locally.

2. **Archive scaffold**
   - Add `docs/archive/README.md` (empty scaffold) and propose archive structure (phase_plans/, audits/, superseded/). Do not move any files.

3. **Non-negotiables**
   - Do not delete any DONE release evidence: `chakraops/docs/releases/*_release_notes.md` and `out/verification/<Release>/notes.md` referenced by RELEASE_CHECKLIST.
   - Follow CLEANUP_POLICY.md: archive-first, never delete release evidence.

4. **Release docs and checklist**
   - Add `chakraops/docs/releases/R24.7.0_requirements.md` and `R24.7.0_release_notes.md`.
   - Update RELEASE_CHECKLIST.md with R24.7.0 section.

5. **Gates and verification**
   - Run backend pytest, frontend tests, frontend build; paste tail outputs into `out/verification/R24.7.0/notes.md` and add a short UAT checklist.

## Out of scope (this release)

- Moving or deleting any files.
- Changing any application or test code.
