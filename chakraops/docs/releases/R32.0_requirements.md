# R32.0 Requirements — Market Data, Earnings, Universe, Freshness

_Program R31–R35, milestone 2. Branch `release/R31-R35-program`. Risk level 4. Status: COMPLETE._

## Approved scope (operator 2026-06-21)

Implement R31 blueprint findings for ORATS reliability and data-input correctness. ORATS remains the sole active options provider; no silent fallback.

## Outcomes

1. Remove hardcoded ORATS token; environment-only authentication. **[DELIVERED — C-1]**
2. ORATS endpoint contracts and error classification. **[DELIVERED — confirmed + `provider_health.classify_provider_failure` + read-only contract validation]**
3. Read-only ORATS smoke with secret redaction. **[DELIVERED]**
4. No silent fallback provider. **[DELIVERED — `provider_health` asserts sole provider, no fallback]**
5. Earnings/event calendar availability or explicit unavailable state. **[DELIVERED — H-4, `event_calendar_status`]**
6. Deterministic weekly universe refresh + history/reasons. **[DELIVERED — M-4, `weekly_refresh` + `refresh_history_store`]**
7. Freshness timestamps + stale-data blocking. **[DELIVERED — M-10, `freshness.stale_data_gate`]**
8. Cache/retry/rate-limit/provider-health observability. **[DELIVERED — `provider_health` + `/api/ui/data-reliability/*`]**
9. Migrate token consumers to `get_orats_token()`; runtime.yaml token field removed. **[DELIVERED — Claude notes]**

## Persistence note (R34 guardrail)

Refresh history uses append-only JSONL — **no database migration in R32**. The deliberate persistence decision (retain vs one controlled migration) is mandated for R34; see `docs/ai/releases/R34.0/RELEASE_PACKET.md`.

## C-1 remediation constraints (honored)

- No token printed, copied to docs, committed, or exposed in tests/evidence.
- Environment-only configuration; `.env.example` placeholder (no real value); `.env`/`*.env` gitignored.
- Clear startup/runtime error when token missing; credentials redacted from logs/reports/tests/evidence.
- No git history rewrite. No silent fallback to fake/alternate options data.

## Gates

Backend 1064 passed / 3 skipped; frontend 311 passed / 18 skipped; build passed. ORATS read-only smoke HTTP 200 / 6939 rows / redacted; secret regression scan clean. Evidence under `out/verification/R32.0/`.
