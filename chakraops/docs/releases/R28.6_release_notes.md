# Release R28.6 — Live open mirror wiring completeness + regression guardrails

**Date:** 2026-03  
**Branch:** release/R28.6  
**Goal:** Wire any missing live OPEN entrypoints; add regression tests so future endpoints cannot bypass mirroring; safe labels and determinism preserved.

---

## What changed

### Backend

- **server.py** — POST `/api/positions/manual-execute`: after successful `manual_execute(body)`, when `account_id != "paper"` and `strategy in ("CSP", "CC")`, call `mirror_live_open_to_unified(position.to_dict())` and `ensure_reconcile_advisory_notification()`. Aligns with R28.5 wiring on the UI route so both entrypoints mirror live options open.
- **ui_routes.py** — No change (shares upsert and UI manual-execute already wired in R28.4/R28.5).
- **positions_unified_store_r279.py** — No change (helpers and reconcile health already in place).

### Tests

- **test_r286_live_open_mirror_wiring_completeness.py** (new):
  - test_live_shares_upsert_wires_mirror_idempotent — POST /api/ui/shares/positions/{symbol} → unified has live_shares_{id}; idempotent.
  - test_ui_manual_execute_wires_mirror_idempotent — POST /api/ui/positions/manual-execute → unified has live_options_{id}; idempotent.
  - test_api_manual_execute_wires_mirror_idempotent — POST /api/positions/manual-execute → unified has live_options_{id}.
  - test_live_open_wiring_does_not_write_decision_latest — mirror + reconcile do not write decision_latest.json.
  - test_reconcile_and_mirror_responses_no_fail_warn_pass — reconcile health and unified rows contain no FAIL/WARN/PASS.

### Frontend

- No code changes. Reconcile block and Positions page already use safe labels.

---

## Discovered live-open entrypoints (complete list)

| Entrypoint | Type | Wired in |
|------------|------|----------|
| POST /api/ui/shares/positions/{symbol} | Shares open | R28.4 |
| POST /api/ui/positions/manual-execute | Options open | R28.5 |
| POST /api/positions/manual-execute | Options open | R28.6 |
| POST /api/ui/positions/{id}/roll | Options close + new open | R28.1 |

---

## Gate counts (evidence)

- R28.6 module: 5 passed in 2.19s.
- Full backend: 951 passed, 1 skipped in 137.60s.
- Frontend tests: 249 passed, 18 skipped.
- Frontend build: 8.69s.
- Evidence: [out/verification/R28.6/notes.md](../../../out/verification/R28.6/notes.md)
