# Release R23.4.8 — Score Used + Plain-English Explainability

**Date:** 2026-02  
**Goal:** Clearly state which score is used (raw vs final capped) and why; add plain-English hold-time explanation (session = one trading day; rough estimate; not a promise). Frontend-only.

---

## Scope

- **In scope:** Score breakdown card "Score used" line and short explanation (capped vs uncapped). Universe score tooltip first line (Final capped / Raw uncapped). Hold-time plain-English in Targets & hold-time card and INFO drawer (session = one trading day, rough estimate, not a promise). Tests and docs.
- **Out of scope:** Backend/API changes. Layout redesign.

---

## UI changes

- **Symbol Diagnostics — Score breakdown:** "Score used: Final score (capped)" or "Score used: Raw score (uncapped)" with 1–2 sentence explanation; then existing Capped by / No caps applied.
- **Universe — Score tooltip:** First line "Score used: Final (capped)" or "Score used: Raw (uncapped)"; existing Capped by and breakdown text unchanged.
- **Hold-time:** Card description and inline plain-English line (one session = one trading day; rough estimate; not a promise). INFO drawer "Hold-time estimate" and "ATR-based hold time" updated with same wording; formulas unchanged.

---

## Tests

- Frontend: R23.4.8 — Score used line when applied_caps present/empty; Hold-time block and drawer include "one trading day" and "not a promise". Existing SymbolDiagnosticsPage score tests passing.

---

## Verification

- Gate output in `out/verification/R23.4.8/notes.md`. UAT: Score breakdown Score used line; Universe score tooltip first line; Hold-time card and (?) drawer text.
