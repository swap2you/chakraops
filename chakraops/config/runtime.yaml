# ChakraOps runtime config.
# The ORATS API token is read from the environment only (ORATS_API_TOKEN).
# Do not store a real token in this file. See .env.example.

orats:
  api_token: "${ORATS_API_TOKEN}"
