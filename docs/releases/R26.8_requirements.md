# R26.8 — Restore full-suite backend green + formalize scoped gate policy

## Objectives

- Fix the failing test `test_ui_positions_post_success_when_within_limits` (pre-existing from R26.7).
- Formalize gate policy in docs (when scoped backend gates are allowed vs when full-suite is mandatory; how to record exceptions).
- Full backend pytest must pass; gate evidence and UAT checklist in `out/verification/R26.8/notes.md`.

## Scope

### A — Fix failing test

1. Reproduce: `cd chakraops && python -m pytest -v --tb=short -k "test_ui_positions_post_success_when_within_limits"`; capture failure reason.
2. Fix minimally: update test expectations/fixtures if behavior is correct and test stale; fix API/constraint logic if behavior is wrong. Keep deterministic; no network; avoid time-based flakiness.
3. Re-run full suite: `cd chakraops && python -m pytest -v --tb=short` — must be all green.

### B — Gate policy in docs

- Update `docs/master/RELEASE_PLAYBOOK.md` with:
  - When scoped backend gates are allowed (doc-only, frontend-only, ops-only).
  - When full-suite is mandatory (backend logic/config, persistence/path changes).
  - How to record exceptions (failing test name, justification, plan to fix next release, time-boxed).

### C — Release artifacts

- `chakraops/docs/releases/R26.8_requirements.md`
- `chakraops/docs/releases/R26.8_release_notes.md`
- Update `chakraops/docs/releases/RELEASE_CHECKLIST.md` with R26.8 section (unchecked until sign-off).
- `out/verification/R26.8/notes.md`: backend pytest tail, frontend tests tail, frontend build tail, UAT checklist.

## Non-negotiables

- No secrets committed.
- Decision artifacts remain code-only; no prose/UI strings persisted; no raw FAIL_/WARN_ tokens in decision_latest.json.
- UI safe labels rule unchanged.
- Full backend pytest MUST pass at the end of this release (no exceptions).
- Gates evidence must be pasted into `out/verification/R26.8/notes.md` + UAT checklist.

## Gates (must run)

- `cd chakraops && python -m pytest -v --tb=short`
- `cd frontend && npm run test -- --run`
- `cd frontend && npm run build`
