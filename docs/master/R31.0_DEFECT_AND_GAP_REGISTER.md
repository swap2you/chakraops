# R31.0 — Defect and Gap Register

_Program R31–R35 · Milestone R31.0 · Read-only audit. Severities: Critical / High / Medium / Low. Every Critical and High item names an owner release. No secret values are printed here._

## Severity definitions

- **Critical** — security exposure or financial-correctness risk that can affect real money or credentials.
- **High** — architectural duplication/integrity risk that undermines trust in data or decisions.
- **Medium** — correctness/UX defect with a clear workaround or limited blast radius.
- **Low** — hygiene, staleness, or cosmetic debt.

---

## Critical

| ID | Title | Evidence (path) | Risk | Owner release |
|----|-------|-----------------|------|---------------|
| C-1 | Hardcoded ORATS API token committed in source | `chakraops/app/core/config/orats_secrets.py` (value not reproduced) | Credential exposure in VCS history; contradicts env-only loading in `app/core/options/providers/orats_client.py` | **R32.0** (rotate + move to env/secret; verify no plaintext remains in working tree). History scrub is forbidden-unless-approved and requires operator decision. |
| C-2 | CSP notional strike fallback to spot | `chakraops/app/core/scoring/position_sizing.py` | When stage-2 strike is missing, sizing uses `spot` as strike estimate → can mis-state cash-secured capital | **R33.0** |

> C-1 immediate action requested of operator: this is a committed secret. Recommend rotating the ORATS token out-of-band. R31.0 (audit-only) does not modify source, so the fix lands in R32.0 under Level 4 review.

---

## High

| ID | Title | Evidence | Risk | Owner release |
|----|-------|----------|------|---------------|
| H-1 | Dual ORATS auth paths (env vs hardcoded) | `providers/orats_client.py` (env) vs `config/orats_secrets.py` (hardcoded); `server.py` lifespan logs "hardcoded, private mode" | Behavior depends on import path; ambiguous source of credential | R32.0 |
| H-2 | Split persistence across `app/data/` vs `data/` vs `out/` | DB inventory in baseline §3.9 | Reconciliation complexity; integrity/backup ambiguity | R34.0 |
| H-3 | ~~Broken frontend `@/data/*` import layer~~ — **withdrawn** | `src/data/` exists; `tsc -b` + build pass in R31.0 evidence | Reclassified: not a defect. Real issue tracked as M-11 (dual API-layer duplication) | — |
| H-4 | Macro-event calendar is a stub | `app/core/environment/event_calendar.py` returns `[]`; `macro_event_gate.py` inert | Macro (FOMC/CPI/NFP) risk gate non-functional in production | R32.0 |
| H-5 | Dual decision/signal stacks and dual ranking | `app/signals/*` + `core/scoring/ranking.py` vs `eval/staged_evaluator.py` + `ranking/service.py`; R33 added canonical `app/core/decision_engine/*` (implemented + tested, not yet live-authoritative) | Divergent results; unclear source of truth for recommendations. **R33 implemented the canonical engine but did NOT make it the authoritative live path; OPEN, reassigned to R34 (live cutover).** | ~~R33.0~~ → R34.0 |

---

## Medium

| ID | Title | Evidence | Owner release |
|----|-------|----------|---------------|
| M-1 | Confidence-band threshold inconsistency | `eval/confidence_band.py` A≥78 vs `eval/decision_artifact_v2.py` `TIER_A_MIN=80` | R33.0 |
| M-2 | Dual API surfaces overlap | `/api/ui/*` (`ui_routes.py`) vs legacy `/api/view/*`,`/api/ops/*` (`server.py`) | R34.0 |
| M-3 | Dual evaluation stores (v1/v2) and dual journals | `evaluation_store.py` vs `evaluation_store_v2.py`; `journal/store.py` vs `journal/journal_store.py` | R34.0 |
| M-4 | No dedicated weekly universe refresh | interval/tier-driven only (`universe_manager.py`, `UNIVERSE_EVAL_MINUTES`) | R32.0 |
| M-5 | Universe base CSV loaded at import | `app/api/data_health.py` | R32.0 |
| M-6 | Nested `<tr>` invalid DOM nesting | `PortfolioPage.tsx`, `JournalPage.tsx`, `ReportsPage.tsx`, `BacktestPage.tsx`, `UniverseAdminPage.tsx` | R34.0 |
| M-7 | Raw gate-token leak paths in UI | `UniversePage.tsx` primary_reason fallback + debug tooltip; `SymbolDiagnosticsPage.tsx` debug tooltips; `TradeTicketPage.tsx` readiness codes | R34.0 |
| M-8 | Strategy profiles not implemented | no selectable Conservative/Balanced/Aggressive/Custom | R33.0 |
| M-9 | Backtest premium model simplified | `app/backtest/engine.py` (`strike*0.005`, synthetic strikes) | R34.0 |
| M-10 | Freshness/cache not surfaced or monitored | `core/data/cache_policy.py` exists but no observability | R32.0 |
| M-11 | Dual frontend API layer (`@/api/*` vs `@/data/*`) | both resolve and build; legacy `src/data/*` used by orphan/legacy modules | R34.0 |
| M-12 | Mixed static/dynamic imports (Vite warning) | build reporter flags `api/client.ts`, `api/queries.ts` | R34.0 |
| M-13 | Bundle > 500 kB warning | R31.0 build: `index-*.js` 637.51 kB (gzip 155 kB) | R34.0 |

---

## Low

| ID | Title | Evidence | Owner release |
|----|-------|----------|---------------|
| L-1 | Python version unpinned | `requirements.txt`/`pyproject.toml` (CI uses 3.11) | R34.0 |
| L-2 | Legacy `main.py`, `app/signals/`, `heartbeat.py` parallel paths | baseline §3.3 | R33.0/R34.0 |
| L-3 | Stale roadmap trackers | `docs/master/PHASE_STATUS.md`, `ROADMAP_2026.md` (R25 era) | R31.0 (note) / R35.0 (close) |
| L-4 | `KNOWN_ISSUES.md` header stale (R30.6) | `docs/master/KNOWN_ISSUES.md` | R31.0 (note) |
| L-5 | Legacy release tree frozen at R27.6 | `docs/releases/` | R35.0 |
| L-6 | Duplicate `frontend/vite.config.js` alongside `.ts` | `frontend/` | R34.0 |
| L-7 | Orphan pages + unmounted CommandBar/CommandPalette | baseline §4.2 | R34.0 |
| L-8 | Duplicate components (`EmptyState`, `StatusBadge`) | `components/` vs `components/ui/` | R34.0 |
| L-9 | Verification path drift (`chakraops/chakraops/out/...`) in old checklist entries | `RELEASE_CHECKLIST.md` (R29.6–R30.0) | R35.0 |
| L-10 | One open TODO (fee model / assignment / auction) | `app/core/journal/store.py:86` | R34.0 |

---

## Carried-forward known issues (from `docs/master/KNOWN_ISSUES.md`)

Issues #1, #2, #3, #5, #6, #7, #8, #9, #10 remain open (mapped above to L/M items). Issue #4 (tracked runtime files) was resolved in R30.7.

## Dependency / process gaps

- **D-1 (High):** R31.0 packet declares dependency "R30.8 merged and tagged," but `CURRENT_STATE.md` shows R30.7 as latest tagged and R30.8 sign-off pending. Operator must confirm R30.8 disposition for this program branch. Owner: operator / R31.0 close.

---

## Owner-release coverage check

Every Critical (C-1, C-2) and High (H-1, H-2, H-4, H-5, D-1) item is assigned an owner release (R32–R34 or operator). H-3 was withdrawn after R31.0 build evidence (reclassified as M-11). Medium/Low items are assigned across R32–R35 per the execution blueprint.
