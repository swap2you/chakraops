# R32.0 Requirements — Market Data, Earnings, Universe, Freshness

_Program R31–R35, milestone 2. Branch `release/R31-R35-program`. Risk level 4. Status: PARTIAL._

## Approved scope (operator 2026-06-21)

Implement R31 blueprint findings for ORATS reliability and data-input correctness. ORATS remains the sole active options provider; no silent fallback.

## Outcomes

1. Remove hardcoded ORATS token; environment-only authentication. **[DELIVERED — C-1]**
2. ORATS endpoint contracts and error classification. **[pre-existing, confirmed]**
3. Read-only ORATS smoke with secret redaction. **[DELIVERED]**
4. No silent fallback provider. **[pre-existing, confirmed]**
5. Earnings/event calendar availability or explicit unavailable state. **[PENDING — H-4]**
6. Deterministic weekly universe refresh + history/reasons. **[PENDING — M-4]**
7. Freshness timestamps + stale-data blocking. **[PENDING — M-10]**
8. Cache/retry/rate-limit/provider-health observability. **[PENDING]**

## C-1 remediation constraints (honored)

- No token printed, copied to docs, committed, or exposed in tests/evidence.
- Environment-only configuration; `.env.example` placeholder (no real value); `.env`/`*.env` gitignored.
- Clear startup/runtime error when token missing; credentials redacted from logs/reports/tests/evidence.
- No git history rewrite. No silent fallback to fake/alternate options data.

## Gates

Backend 1023 passed / 3 skipped; frontend 308 passed / 18 skipped; build passed. Evidence under `out/verification/R32.0/`.
