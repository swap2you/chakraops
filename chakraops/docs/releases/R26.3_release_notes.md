# R26.3 Release Notes — Daily Operator Workflow ("Today" command center)

## Summary

- **Today page:** Single workflow at /today with Run/Refresh, Action Needed, Trade Ticket queue (localStorage), Journal checkpoint, Notifications inbox. Consistent as_of where possible; safe labels only.
- **Backend:** GET /api/ui/today/summary — lightweight summary (latest_run_ts, cadence, guardrails, notifications health, earnings probe, counts).
- **Frontend:** TodayPage; route /today; nav "Today"; ticket queue in localStorage; no FAIL/WARN in UI.

## Backend

- GET /api/ui/today/summary — latest_run_ts / as_of, cadence, guardrails health, notifications health, earnings_probe (one symbol), action_needed_count, notifications_new_count.

## Frontend

- TodayPage.tsx; route /today; sidebar "Today"; Run evaluation button; Action Needed with Ticket links; Ticket queue (add/remove/done, localStorage); Journal checkpoint (entries today, add entry, missing-journal reminder); Notifications (NEW list, Ack/Archive, Ack all NEW, Archive all ACKED).

## Tests

- test_r263_today_summary.py (schema, no FAIL_/WARN_); TodayPage tests (sections, queue, notifications mutations, no FAIL/WARN in DOM).
