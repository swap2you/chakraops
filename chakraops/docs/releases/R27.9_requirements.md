# R27.9 — Unified Positions DB v1 (read-only aggregation first)

**Branch:** release/R27.9  
**Manual-only.** No broker automation. Phase 8+ Epic 8.1.

## Objectives

- New SQLite store under DATA_DIR (data/positions.db) with schema positions_open / positions_closed.
- Read-only builder that aggregates live shares (holdings_db / share_positions), live options (tracked positions store), and paper positions into a unified list with stable ordering and stable IDs.
- GET /api/ui/positions/unified with filters (state, include_paper, instrument_type, symbol); response safe labels only.
- System-health block positions_unified (open_count, closed_count, last_build_ts).
- Frontend: Positions section or page with filters (Open/Closed, Live/Paper, Type, Symbol) and columns (Symbol, Type, Qty, Opened, Mark/Unrealized, Ticket link, Journal link).

## Non-negotiables

- Decision artifacts remain code-only; never persist prose/UI strings or raw FAIL_/WARN_.
- UI safe labels only; never show raw FAIL/WARN.
- Determinism: unified list stable ordering and stable IDs.
- No broker automation; manual-only.
- Gates pass; evidence in out/verification/R27.9/notes.md + UAT checklist.

## Scope

1. **Backend:** positions_unified_store_r279.py (schema, path override, build from holdings_db + positions store + paper store); GET /api/ui/positions/unified; system-health positions_unified block.
2. **Frontend:** Unified Positions page/section with filters and columns; no FAIL/WARN in DOM.
3. **Tests:** Backend deterministic ordering, filters, no FAIL_/WARN_; frontend renders and no FAIL/WARN in document.
4. **Docs:** This file, R27.9_release_notes.md, RELEASE_CHECKLIST R27.9 block, out/verification/R27.9/notes.md.

## UAT checklist

- [ ] Unified positions page loads and shows stable ordering.
- [ ] Filters (open/closed, live/paper, type) work.
- [ ] Ticket/Journal links open correct pages.
- [ ] No FAIL/WARN visible anywhere.
- [ ] Gates pasted (pytest, frontend test, frontend build).
- [ ] Grep proof: no FAIL_|WARN_ in out/decision_latest.json (if file exists).
