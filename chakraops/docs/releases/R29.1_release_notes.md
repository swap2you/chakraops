# Release R29.1 — Positions Trust Banner v1 (Integrity strip)

**Date:** 2026-03  
**NO GIT.** Manual-only.

**Goal:** Reconcile status and diff summary on /positions with safe-label Integrity strip; View diff details (inline), Rebuild, Switch to Computed/Stored. No new APIs.

---

## User-visible changes

- **Positions page — Integrity strip (above table):**
  - **When Source = Stored:** Reconcile status (OK or Review). When Review: diff counts (missing/extra/mismatched), "View diff details" (expand inline list with kind, symbol, type, id, fields_diff), "View positions" link per item to filtered Positions, "Rebuild unified positions" (confirm modal), "Switch to Computed".
  - **When Source = Computed:** "Integrity: Computed (authoritative)" and "Switch to Stored" link.
- All labels safe (OK/Review). No FAIL/WARN/PASS in UI.

## API

- No new endpoints. Uses existing GET /api/ui/system-health (positions_unified_reconcile), GET /api/ui/positions/unified/reconcile-diff, POST rebuild.

## Tests and evidence

- Backend: test_r291_* (4 tests) — positions blocks and reconcile-diff no forbidden tokens; no decision_latest write.
- Frontend: PositionsPage R29.1 tests (Integrity strip, diff expand, Rebuild, Switch, no forbidden tokens).
- Evidence: **out/verification/R29.1/notes.md**.
