# Release R28.5 — Live options OPEN mirror wiring

**Date:** 2026-03  
**Branch:** release/R28.5  
**Goal:** Wire live OPTIONS OPEN mirror on manual-execute (and any other live options create path); reconcile health already includes live options counts; safe labels only.

---

## What changed

### Backend

- **ui_routes.py** — POST `/positions/manual-execute`: after successful `manual_execute(body)`, when `account_id != "paper"` and `strategy in ("CSP", "CC")`, call `mirror_live_open_to_unified(position.to_dict())` and `ensure_reconcile_advisory_notification()`. No notification on system-health fetch.
- **positions_unified_store_r279.py** — No change; `mirror_live_open_to_unified` (options) and `get_positions_unified_reconcile_health()` (live_options_open_count, unified_open_live_options_count) already in place from R28.1/R28.4.

### Tests

- **test_r285_positions_unified_live_options_open_wiring.py** — test_live_options_upsert_wires_mirror_to_unified_idempotent, test_reconcile_health_includes_live_options_open_counts_and_safe_labels, test_wiring_does_not_write_decision_latest.

### Frontend

- No change. Reconcile block typings already include live options keys (R28.4).

---

## Gate counts (evidence)

- R28.5 module: 3 passed in 0.58s.
- Full backend: 946 passed, 1 skipped in 404.58s.
- Frontend tests: 249 passed, 18 skipped.
- Frontend build: 7.55s.
- Evidence: [out/verification/R28.5/notes.md](../../../out/verification/R28.5/notes.md)
