# R31.0 Release Notes — Repository, Product, and Live-Data Baseline

_Program R31–R35, milestone 1 on `release/R31-R35-program`. Documentation/audit only; no source changes._

## Summary

R31.0 establishes the trusted baseline for the five-release recovery program. It delivers a repository/product architecture map, a ranked defect and gap register, and a file-level execution blueprint for R32–R35, validated by the AGENTS.md baseline gates and a read-only ORATS smoke.

## What changed

- Added `docs/master/R31.0_REPOSITORY_PRODUCT_BASELINE_AUDIT.md` — backend/frontend/ops architecture map and capability baseline.
- Added `docs/master/R31.0_DEFECT_AND_GAP_REGISTER.md` — Critical/High/Medium/Low register; every Critical/High has an owner release.
- Added `docs/master/R31.0_EXECUTION_BLUEPRINT.md` — file-level targets and `[APPROVAL]` gates per downstream milestone.
- Updated program/release tracking: `docs/ai/PROGRAM_STATUS.md`, `docs/ai/releases/R31.0/{STATUS,TOOL_LOG}.md`, `docs/master/CURRENT_STATE.md`, `RELEASE_CHECKLIST.md`.

## Gate results

| Gate | Result |
|------|--------|
| Backend pytest | 1018 passed, 2 skipped (259.79s) |
| Frontend tests | 308 passed, 18 skipped |
| Frontend build | Passed (tsc -b clean; vite 9.56s) |
| ORATS read-only smoke | PASS — HTTP 200, 6939 rows, redacted |

Evidence: `out/verification/R31.0/`.

## Key findings

- **Critical:** committed ORATS token in `chakraops/app/core/config/orats_secrets.py` (C-1; rotate out-of-band, fix in R32.0); CSP notional strike-fallback to spot (C-2; R33.0).
- **High:** dual ORATS auth paths (H-1); split persistence across `app/data/`/`data/`/`out/` (H-2); macro-event calendar stub (H-4); dual decision/ranking stacks (H-5).
- **Correction:** earlier "broken `@/data` imports" hypothesis was refuted — `src/data/` exists and the build/tsc pass; reclassified as M-11 (dual API-layer duplication).

## Open items for operator

- D-1: confirm R30.8 disposition (CURRENT_STATE shows R30.7 latest tagged; R30.8 sign-off pending) before R32.0.
- C-1: ORATS token rotation.

## Workflow note

Program runs on a single branch with five milestone commits and one final PR after R35.0. R31.0 is committed as milestone 1; no per-milestone merge or tag.
