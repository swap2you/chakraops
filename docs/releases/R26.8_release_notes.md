# Release R26.8 — Restore full-suite backend green + formalize scoped gate policy

**Date:** 2026-02  
**Branch:** release/R26.8  
**Goal:** Fix pre-existing failing UI positions test, formalize when scoped vs full backend gates apply, and record gate evidence.

---

## Scope

### Test fix

- **test_ui_positions_post_success_when_within_limits** was failing with 409 due to time-dependent wheel policy (DTE from `date.today()`). Fix: mock `evaluate_wheel_policy` in the test so the test is deterministic and asserts account/limits behavior only.
- No production code changed; test-only change in `chakraops/tests/test_ui_routes.py`.

### Docs

- **RELEASE_PLAYBOOK.md** — New section 1.4 "Gate policy: when scoped vs full-suite":
  - When scoped backend gates are allowed (doc-only, frontend-only, ops-only).
  - When full-suite is mandatory (backend logic/config, persistence/path changes).
  - How to record exceptions (failing test name, justification, plan to fix next release, time-boxed).

### Release artifacts

- R26.8_requirements.md, R26.8_release_notes.md.
- RELEASE_CHECKLIST.md updated with R26.8 section.
- out/verification/R26.8/notes.md with gate tails and UAT checklist.

---

## File list

- `chakraops/tests/test_ui_routes.py` — Mock `evaluate_wheel_policy` in `test_ui_positions_post_success_when_within_limits`.
- `chakraops/docs/master/RELEASE_PLAYBOOK.md` — Section 1.4 gate policy.
- `chakraops/docs/releases/R26.8_requirements.md`, `R26.8_release_notes.md`, `RELEASE_CHECKLIST.md`.
- `out/verification/R26.8/notes.md` — Gate evidence and UAT.

---

## Gates

- Backend: full suite `cd chakraops && python -m pytest tests -v --tb=short` — 866 passed, 1 skipped.
- Frontend tests: `cd frontend && npm run test -- --run` — 208 passed, 18 skipped.
- Frontend build: `cd frontend && npm run build` — built in 8.29s.
