# R24.1 — Handoff for ChatGPT (or other) Agent

**Purpose:** Give this document (and optionally the referenced files) to an AI agent to verify or continue R24.1. Implementation is **complete**; gates are pasted in verification notes. Use OOM-safe steps below if working in a memory-constrained environment.

---

## 1. Original prompt (reference)

You are working in the ChakraOps repo. Continue R24.1 "Actionable Trading Workflow v1" from the current checkpoint. Part 1 is already implemented (next_action_code + next_action_details request-time only). Implement the remaining Parts 2–4 exactly and record gates in out/verification/R24.1/notes.md.

**NON-NEGOTIABLES**
- Decision JSON artifacts remain code-only. Do not persist prose/UI strings/panels and do not persist raw FAIL_/WARN_ anywhere in decision_latest.json or archives.
- UI must never display raw FAIL_/WARN_ codes.
- SDLC gates must pass and be pasted into out/verification/R24.1/notes.md.
- Repo hygiene: out/ only whitelisted artifacts; no secrets; no scratch.

**Gates (already run and pasted):**
- Backend: `cd chakraops; python -m pytest tests/ -v --tb=short`
- Frontend tests: `cd frontend; npm run test -- --run`
- Frontend build: `cd frontend; npm run build`

---

## 2. Implementation status (COMPLETE)

| Part | Requirement | Status | Location |
|------|-------------|--------|----------|
| **Part 2.1** | Slack per-symbol Action block (OPTIONS: strategy, next_action_code, rationale, sizing, key levels, contract_key/option_symbol, delta_best, DTE, bid/ask/spread_pct; SHARES: next_action_code, rationale, spot, entry zone, stop, T1, shares/cost, risk %) | Done | `chakraops/app/core/alerts/slack_dispatcher.py` — `build_actionable_message_r241()` |
| **Part 2.2** | FORBIDDEN_IN_SLACK: no FAIL_, WARN_, api key/token, paths, stack traces; sanitization; tests | Done | Same file: `FORBIDDEN_IN_SLACK`, `_sanitize_slack_text`; tests in `test_r241_slack_actionable_messages.py` |
| **Part 2.3** | Dedupe: same (symbol, strategy, action, contract/size) within N min unless severity changes; state under chakraops/data/ | Done | `slack_dispatcher.py`: `should_send_actionable_message()`, `_actionable_dedupe_key()`, state `chakraops/data/slack_actionable_dedupe.json`; env `SLACK_ACTIONABLE_DEDUPE_MINUTES` (default 15) |
| **Part 2.4** | Backend tests: required fields, no forbidden substrings, dedupe suppresses repeat / allows when action changes | Done | `chakraops/tests/test_r241_slack_actionable_messages.py` (5 tests) |
| **Part 3.1** | GET /api/ui/action-needed: top_options (max 5), top_shares (max 5); item: symbol, strategy, next_action_code, rationale_lines, key_number, tab, accordion id; recently_changed stub | Done | `chakraops/app/api/ui_routes.py`: `@router.get("/action-needed")`, `_action_needed_item_from_diagnostics()`; returns `top_options`, `top_shares`, `recently_changed` |
| **Part 3.2** | Dashboard Action Needed calls API; Options (5) + Shares (5); row: symbol, badge, rationale, key number; link to symbol-diagnostics?symbol=...&tab=...&accordion=...; Symbol page accordion from URL | Done | `frontend/src/pages/DashboardPage.tsx`: `useActionNeeded()`, rows with href; `frontend/src/pages/SymbolDiagnosticsPage.tsx`: `accordionFromUrl`, `initialAccordionId` |
| **Part 3.3** | Frontend tests: Action Needed options/shares rows from mock; deep-link has tab+accordion; Symbol page accordion from param | Done | `frontend/src/pages/DashboardPage.test.tsx` (mock useActionNeeded, assert rows and link href); SymbolDiagnosticsPage tests in score test file |
| **Part 4.1** | Document DECISION_ARCHIVE_MAX / DECISION_HISTORY_KEEP, where implemented, how to verify | Done | `chakraops/docs/releases/R24.1_release_notes.md`, `RELEASE_CHECKLIST.md` |
| **Part 4.2** | Paste gate outputs; UAT checklist in notes.md | Done | `out/verification/R24.1/notes.md` |

---

## 3. Files changed (backend, frontend, tests, docs)

**Backend**
- `chakraops/app/core/alerts/slack_dispatcher.py` — R24.1 builder, FORBIDDEN_IN_SLACK, dedupe, state path
- `chakraops/app/api/ui_routes.py` — GET /api/ui/action-needed, _action_needed_item_from_diagnostics (top_options, top_shares, accordion_id)

**Frontend**
- `frontend/src/api/queries.ts` — actionNeededPath(), ActionNeededItem, ActionNeededResponse, useActionNeeded()
- `frontend/src/pages/DashboardPage.tsx` — Action Needed card uses useActionNeeded(), top_options/top_shares, deep-link with tab + accordion_id
- `frontend/src/pages/SymbolDiagnosticsPage.tsx` — accordionFromUrl, initialAccordionId passed to ExecutionConsole

**Tests**
- `chakraops/tests/test_r241_slack_actionable_messages.py` — required fields, forbidden substrings, sanitize, dedupe key, dedupe suppress/allow
- `frontend/src/pages/DashboardPage.test.tsx` — useActionNeeded mock, action-needed card options/shares rows, link href contains tab and accordion

**Docs**
- `chakraops/docs/releases/R24.1_requirements.md` — source of truth
- `chakraops/docs/releases/R24.1_release_notes.md` — Parts 1–4, retention, tests, gate pointer
- `chakraops/docs/releases/RELEASE_CHECKLIST.md` — R24.1 section all [x]
- `out/verification/R24.1/notes.md` — gate outputs pasted, UAT checklist

---

## 4. Proof points (tests that demonstrate requirements)

| Requirement | Test(s) | Command to run only that test |
|-------------|---------|-------------------------------|
| Dedupe suppresses repeat, allows when action changes | test_dedupe_suppresses_repeat_allows_when_action_changes | `cd chakraops; python -m pytest tests/test_r241_slack_actionable_messages.py::test_dedupe_suppresses_repeat_allows_when_action_changes -v --tb=short` |
| Forbidden substrings never in message | test_r241_message_never_forbidden, test_sanitize_removes_forbidden | `cd chakraops; python -m pytest tests/test_r241_slack_actionable_messages.py::test_r241_message_never_forbidden tests/test_r241_slack_actionable_messages.py::test_sanitize_removes_forbidden -v --tb=short` |
| Dashboard Action Needed rows and deep-link | DashboardPage.test.tsx (action-needed card, link href) | `cd frontend; npm run test -- --run DashboardPage` |
| Symbol accordion from URL | SymbolDiagnosticsPage (initialAccordionId) | `cd frontend; npm run test -- --run SymbolDiagnosticsPage` |

---

## 5. Gate results and where pasted

All three gates have been run and **outputs are pasted** in:

**File:** `out/verification/R24.1/notes.md`

- **Backend:** 737 passed, 2 skipped (incl. test_r241_slack_actionable_messages.py all 5 passed).
- **Frontend tests:** 21 files passed, 161 tests passed, 18 skipped.
- **Frontend build:** Success, built in 7.25s.

Re-run gates if you need to re-verify:
```bash
cd chakraops; python -m pytest tests/ -v --tb=short
cd frontend; npm run test -- --run
cd frontend; npm run build
```

---

## 6. OOM-safe steps (work in small chunks)

If the agent or editor has limited memory, do **one step per session**; open only the files listed.

| Step | Task | Files to open (only these) | Command |
|------|------|-----------------------------|--------|
| A | Verify backend gate | (none; terminal only) | `cd chakraops; python -m pytest tests/ -v --tb=short` |
| B | Verify frontend tests | (none) | `cd frontend; npm run test -- --run` |
| C | Verify frontend build | (none) | `cd frontend; npm run build` |
| D | Confirm gate outputs in notes | `out/verification/R24.1/notes.md` | Read; no command |
| E | Confirm Slack Part 2 | `chakraops/app/core/alerts/slack_dispatcher.py`, `chakraops/tests/test_r241_slack_actionable_messages.py` | grep for build_actionable_message_r241, FORBIDDEN_IN_SLACK, should_send_actionable |
| F | Confirm action-needed API | `chakraops/app/api/ui_routes.py` (lines ~3467–3615) | grep for action-needed, top_options, accordion_id |
| G | Confirm Dashboard + Symbol | `frontend/src/pages/DashboardPage.tsx`, `frontend/src/pages/SymbolDiagnosticsPage.tsx` (lines ~88–95, 191–194) | grep for useActionNeeded, accordionFromUrl, initialAccordionId |
| H | Run UAT checklist | — | Manual: eval → Action Needed; deep-link; Slack dedupe; grep decision_latest.json for FAIL_/WARN_ |

---

## 7. UAT checklist (from notes.md)

- [ ] Run eval; confirm Dashboard Action Needed shows both options + shares.
- [ ] Confirm deep-link opens correct tab + accordion (from Action Needed row to symbol page).
- [ ] Confirm Slack sends actionable per-symbol block and dedupe works (run twice within N minutes; second suppressed).
- [ ] Confirm decision_latest.json remains code-only: `grep -E "FAIL_|WARN_|/home/|C:\\\\" out/decision_latest.json` returns no matches.

---

## 8. Marking R24.1 complete

- **RELEASE_CHECKLIST.md:** R24.1 section is already [x] for Parts 1–4, Tests, Requirements, Release notes, Verification.
- **Verification:** Gate outputs are in `out/verification/R24.1/notes.md`. UAT can be run manually when ready.
- **No further implementation required** for the original prompt; only optional UAT execution and any follow-up fixes if gates fail on a different machine.
