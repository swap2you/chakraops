# R28.7 Verification — Unified Positions Rebuild v1 + Diagnostics UI action

**Copy this content to `out/verification/R28.7/notes.md`.** (out/ may be gitignored; this file is the canonical evidence.)

---

## Gate commands (exact from prompt — run from repo root)

1. `cd chakraops/chakraops && python -m pytest tests/test_r287_* -v --tb=short`
2. `cd chakraops/chakraops && python -m pytest tests -v --tb=short`
3. `cd chakraops/frontend && npm run test -- --run`
4. `cd chakraops/frontend && npm run build`

(On Windows PowerShell, gate 1 may need explicit file: `tests/test_r287_positions_unified_rebuild.py` if glob does not expand.)

---

## Gate 1 — R28.7 backend tests (test_r287_*)

```
tests/test_r287_positions_unified_rebuild.py::test_rebuild_is_idempotent_and_deterministic_counts PASSED [ 25%]
tests/test_r287_positions_unified_rebuild.py::test_rebuild_endpoint_safe_labels_no_fail_warn_pass PASSED [ 50%]
tests/test_r287_positions_unified_rebuild.py::test_rebuild_does_not_write_decision_latest PASSED [ 75%]
tests/test_r287_positions_unified_rebuild.py::test_rebuild_state_file_has_no_forbidden_tokens PASSED [100%]

============================== 4 passed in 4.17s ==============================
```

---

## Gate 2 — Full backend tests

```
======================= 953 passed, 3 skipped in 44.27s =======================
```

---

## Gate 3 — Frontend tests

```
 Test Files  31 passed | 2 skipped (33)
      Tests  252 passed | 18 skipped (270)
   Duration  54.77s
```

---

## Gate 4 — Frontend build

```
✓ 1497 modules transformed.
✓ built in 11.96s
```

---

## UAT checklist

- [x] Reconcile shows OK/Review safe labels
- [x] Rebuild button works (confirm, triggers rebuild)
- [x] After rebuild, unified positions list loads and looks consistent
- [x] No raw FAIL/WARN/PASS in UI

---

## Grep proof (record terminal outputs)

```bash
ls -l out/decision_latest.json || true
# if present:
grep -E "FAIL_|WARN_" out/decision_latest.json || true
grep -R -n -E '\b(FAIL|WARN|PASS)\b' out/positions_unified_rebuild_state.json out/mark_refresh_state.json out/portfolio_risk_notify_state.json || true
```

If files are absent (expected without prior runs), note explicitly: "Files absent; expected without runs."
